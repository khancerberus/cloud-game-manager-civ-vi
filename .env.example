DATABASE_URL=
# Set to true to create the PostgreSQL database and sync the schema automatically on startup.
# This is best suited for local or first-time setup; avoid enabling it in production.
DATABASE_AUTO_CREATE=
DS_WEBHOOK_URL=
PUBLIC_HOST=